# Server-only credentials. Never use NEXT_PUBLIC_ for these keys.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Enable only after SQL migration, abuse protection and privacy review.
CONTACT_ENABLED=false
APP_ORIGIN=http://localhost:3001
# Optional future gallery adapter, no Cloudinary account is required for local assets.
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
